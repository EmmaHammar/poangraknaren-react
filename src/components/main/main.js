import React, { Component } from "react";
import Form from "./Form";

class Main extends Component {
    render() {
        return(
        <>
            <Form />
        </>
        );
    };
};

export default Main;